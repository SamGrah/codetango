# Dependencies
node_modules/

# Build output (Astro builds inside astro/, which has its own .gitignore too)
dist/

# macOS
.DS_Store

# Stray repo-local agent state (real memory lives in ~/.claude)
.claude/projects/

# Python virtualenvs (in case one is ever created locally)
venv/
.venv/
__pycache__/
*.py[cod]
